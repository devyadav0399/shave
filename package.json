{
  "name": "shave",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --elide-lines=0 --env-file=.env run --filter shave-server dev & bun --env-file=.env run --filter shave-client dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "open-graph-scraper": "^6.11.0"
  }
}
